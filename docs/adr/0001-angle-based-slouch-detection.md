# 耳ー肩の角度ベースで猫背判定する

垂直距離（Y座標差分）ではなく、肩中点と耳を結ぶ線が垂直線と成す角度で猫背を判定する。角度ベースにより、頭が下がる猫背と頭が前に出る猫背（ストレートネック）の両方を1つの指標で検知できる。垂直距離では前方への頭の出を検知できないため、デスクワーカーの2大猫背パターンを網羅するには不十分だった。

## Considered Options

- 垂直距離のみ：シンプルだが前方への頭の出を見逃す
- 垂直＋水平の両方：2パターンを網羅するが閾値が2つになり感度調整が複雑
- 角度ベース：1つの指標で2パターンをカバー（採用）
