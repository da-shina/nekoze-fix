# 斜めカメラ角度設置に対応する

カメラが正面ではなくテーブル上の前方斜め45度に設置されることを前提に要件を設計する。遠側のキーポイントは信頼度が低いため、近側の耳と肩の関係を主指標とする。確定判定時間を3秒から5秒に延長してノイズを吸収する。キャリブレーション時にカメラ角度を吸収するため、正面設置でも斜め設置でも動作する。

## Considered Options

- 正面設置に変更：ユーザーの実際の使用方法と合わない
- 斜め45度前提で要件再設計（採用）：ユーザーの実際の設置方法に合わせる
- 3D姿勢推定で斜め対応：iOS 15+が必要で実装複雑度が上がる
