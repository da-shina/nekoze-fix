# NekozeFix

iPhone/iPadのフロントカメラでデスクワーカーの猫背をリアルタイム検知し、音で通知するiOS/iPadOSネイティブアプリ。

## Language

**猫背（Slouching）**:
近側の耳と肩の関係に基づく角度が、キャリブレーション時の基準角度から閾値以上に増加した状態。斜めカメラ角度でも安定して検知できるよう近側キーポイントを主指標とする。
_Avoid_: 姿勢不良、悪い姿勢

**基準姿勢（Reference Posture）**:
キャリブレーション時に5秒間連続安定して検出された、ユーザーの正しい姿勢における近側の耳と肩の角度。カメラ設置角度（正面・斜め）を吸収する。プロセス内メモリに保持し、永続化しない。
_Avoid_: 正しい姿勢、初期姿勢

**近側キーポイント（Near-side Keypoint）**:
カメラに近い側の肩と耳のキーポイント。画像上で画面左寄りの肩（x座標が小さい側）を近側とする。片側のみ検出時はその側を自動的に近側として扱う。
_Avoid_: 手前側、近い側

**確定猫背判定（Confirmed Slouch）**:
猫背状態が3秒間連続して検知された時に確定する判定。確定判定の初回に通知音が鳴り、猫背継続中は音声1ループ長の間隔で繰り返し再生される。
_Avoid_: 猫背検知、猫背判定（曖昧）

**画面暗転モード（Dim Mode）**:
フォアグラウンドで監視を継続しつつ、画面輝度を0.0（全黒）に設定し、isIdleTimerDisabled = true でwake lock を維持するモード。カメラと姿勢検知は動作し続ける。暗転中は人物なし（personMissing）の表示を抑制し、復帰時に見せる。
_Avoid_: スリープモード、省電力モード

**判定閾値（Slouch Threshold）**:
基準角度からの角度増加量がこの値以上で猫背候補となる度数。3.0〜20.0度の範囲、デフォルト5.0度、ステップ0.5度。値が小さいほど厳しい判定になる。旧・感度（0.0-1.0）抽象層は廃止され、UI でも「閾値」度数を直接表示する。
_Avoid_: 感度

**キャリブレーション（Calibration）**:
ユーザーの基準姿勢を登録する手順。5秒間連続で姿勢が安定したら自動完了。途中で姿勢が崩れたら蓄積をリセットして再開する。カメラの設置角度（正面・斜め45度等）を吸収し、正面でも斜めでも動作する。
_Avoid_: セットアップ、初期設定

**通知音（Alert Sound）**:
猫背確定判定時に再生される内蔵音。マナーモードでも再生される。猫背継続時は前回通知から音声1ループ長（`AVAudioPlayer.duration` に追従、現音源 `usagi-to-kame.caf` は約14.2秒）の間隔で繰り返し再生される。
_Avoid_: アラート、警告音

**回転中（Rotating）**:
デバイスの向き変更検出から完了までの間の状態。猫背判定を停止し、蓄積中のゲートは保持する（3秒未満の中途状態はリセットされない）。人物なしの表示も抑制する。
_Avoid_: ローテーション中

**人物なし（Person Missing）**:
カメラフレーム内に人物が検出されない状態。Vision が null を返し、0.5秒の猶予期間（瞬間的な検出抜けの吸収）を経た時点で判定される。暗転モード中および回転中は表示を抑制する。
_Avoid_: 人物未検出

**耳-肩角度（Ear-Shoulder Angle）**:
肩から耳へ向かうベクトルと、画像上向きの垂直ベクトル(0, 1)のなす鋭角（0〜90度）。この角度が基準角度 + 閾値以上増加すると猫背候補となる。座標系は画像左下原点、x右向き、y上向き。
_Avoid_: 傾斜角、垂直角度

**姿勢崩れ（Posture Instability）**:
キャリブレーション中に角度が直近7サンプルの中央値から5度以上離れるか、人物検出が途絶えた状態。この状態で蓄積がリセットされる。
_Avoid_: 姿勢逸脱